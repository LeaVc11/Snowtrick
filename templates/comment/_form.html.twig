<section class="container-fluid background-layout-stairs">
    <div class="row justify-content-center">
        <div class="col-md-8 ">
            <div class="card shadow-lg rounded my-5">
                <div class="jumbotron vh-100card-img-top">
                    <div class="p-1 p-md-4 text-white rounded">
                        <div class="row justify-content-center">
                            {{ form_start(form) }}
                           <p class="text-primary fw-bold text-decoration-underline"> Commentaire :</p>
                            <br>
                            {{ form_end(form) }}
                            <div class="d-flex justify-content-center align-items-center mt-3">
                                <div class="row">

                                    <div class="col m-6">
                                        <a href="{{ path('app_comment') }}"
                                           class="btn btn-sm btn-outline-warning border border-info border-3"><img
                                                    src="{{ asset('assets/images/logoReturn.png') }}" height="18px"
                                                    alt="Return"></a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>




